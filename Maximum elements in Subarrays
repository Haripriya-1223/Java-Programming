import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);

        List<Integer> list = new ArrayList<>();

        // Read the array elements from the first line
        String line = sc.nextLine();
        String[] arr = line.split(" ");

        for (String s : arr) {
            list.add(Integer.parseInt(s));
        }

        int k = sc.nextInt();

        int n = list.size();

        for (int i = 0; i <= n - k; i++) {
            int max = list.get(i);

            for (int j = i; j < i + k; j++) {
                if (list.get(j) > max) {
                    max = list.get(j);
                }
            }

            System.out.print(max);
            if (i < n - k) {
                System.out.print(" ");
            }
        }

        sc.close();
    }
}
